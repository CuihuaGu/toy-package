import b from "./app";
/**
 * 返回 hello 开头的字符串
 * @param str - input string
 * @returns 'hello xxx'
 * @example
 * ```ts
 * a('ts') => 'hello ts'
 * ```
 *
 * @beta
 * @author ziming
 */
function a() {
  console.log(1234);
  return 1234;
}

export default a;
export { b };
